const f = require('./somefuncs');

/**
 * the benefits of JSDoc is in it's auto-complete messaging and info.
 * so start typing out the functions and you'll see the difference between
 * a function w/ JSDoc and the one w/out JSDoc.
 */

console.log(f.add(12, 34));
console.log(f.subtract(56, 78));

