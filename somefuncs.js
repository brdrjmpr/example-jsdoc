// see https://jsdoc.app/tags-param for easy docs for parameters
//    callbacks, returns, type, typedef at bottom of page

/**
 * @param {number} item1 any numerical value
 * @param {number} item2 more example text
 * @returns {string}
 */
function add(item1, item2) {
    return((item1 + item2).toString());
 }
 
 
 function subtract(x, y) {
    return x - y;
 }

 module.exports = { add, subtract};

// added a few lines at the bottom
// added a few lines at the bottom
// added a few lines at the bottom
